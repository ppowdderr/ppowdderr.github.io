# v13 — Contact Sheet

The index is a strip of developed 35mm film: sprocket holes run along both edges (CSS radial-gradient tiling), and each essay is a 3:2 frame whose "image" is its own opening words, dim as a latent print until the loupe (hover/focus) brings them up. Chinagraph-orange frame numbers (`▶ 03A`) mark the rebate. Opening a frame enlarges it into a serif print. Old pattern: the photographer's contact sheet. New pattern: `aspect-ratio` frames in an `auto-fill` grid, text-as-image glimpses.
