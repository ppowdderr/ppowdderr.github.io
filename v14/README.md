# v14 — Endgame

The index is a chess study: an 8×8 diagram where each essay stands as an ivory piece (♔ ♕ ♖ …) on a square chosen deterministically from its slug, with the coordinate whispered in the corner and a move list beneath (`e4 On Walking · 2026-05-12`). Arrow keys walk the occupied squares. Opening a piece gives the annotated study with earlier/later move navigation. Old pattern: the printed chess diagram. New pattern: keyboard-navigable CSS grid, slug-hashed positioning.
