# v9 — Vitrine

The index is a museum wall of accession labels (`IT.2026.003`, acquired / medium / provenance as a `<dl>`), each lit by a CSS radial pin-light. Opening a case pins the label sticky beside the essay, which is set in serif — the artefact, distinct from its sans-serif label. Old pattern: museum object labels. New pattern: CSS grid `auto-fill` wall + `:has()` hover on the whole label.
